import { shallowMount } from '@vue/test-utils'
import Home from '@/components/Home'

describe('Home', () => {
  let wrapper

  beforeEach(() => {
    wrapper = shallowMount(Home)
  })

  test('', () => {

  })
})
